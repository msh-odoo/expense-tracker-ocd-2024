<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
    <t t-name="expense_tracker.ExpenseForm">
        <div class="expense-form">
            <FormView data="state.data" t-on-submit="addExpense">
                <!-- <t t-set="formAction" t-value="addExpense"/> -->
                <t t-set-slot="title">
                    <h2><t t-out="this.title"/></h2>
                </t>
            </FormView>
            <!-- <div class="d-flex">
                <form class="w-100" t-on-submit.prevent="addExpense">
                    <div class="form-group">
                        <label>Description:</label>
                        <input class="form-control" type="text" t-model="state.expense_data.expense.name" required="true"/>
                    </div>
                    <div class="form-group">
                        <label>Date:</label>
                        <input class="form-control" type="date" t-model="state.expense_data.expense.date" required="true" />
                    </div>
                    <div class="form-group">
                        <label>Amount:</label>
                        <input class="form-control" type="number" t-model="state.expense_data.expense.amount" required="true" />
                    </div>
                    <div class="form-group">
                        <label>Category:</label>
                        <select class="form-control" t-model="state.category">
                            <t t-foreach="state.expense_data.categories" t-as="category" t-key="category.id">
                                <option t-att-value="category.id"><t t-out="category.name"/></option>
                            </t>
                        </select>
                    </div>
                    <button class="btn btn-primary" type="submit">Add Expense</button>
                </form>
            </div> -->
        </div>
    </t>
</templates>
